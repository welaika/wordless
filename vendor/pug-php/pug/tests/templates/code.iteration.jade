
- var items = array(1,2,3)

ul
  for item, i in items
    li(class='item-#{$i}')= item

ul
  each item, i in items
    li= item

nums = array(1, 2, 3)
letters = array('a', 'b', 'c')

ul
  for l in letters
    for n in nums
      li #{$n}: #{$l}
