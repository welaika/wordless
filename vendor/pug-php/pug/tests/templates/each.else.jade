
users = array()

ul
  for user in users
    li= user
  else
    li no users!


users = array('tobi', 'loki')

if users
  ul
    for user in users
      li= user
    else
      li no users!

each _x in [1,2,3]
  - print_r(_x)
